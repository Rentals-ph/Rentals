; PHP configuration for upload limits (listing assistant: up to 30 images × 10MB)
; Used by PHP-FPM and some shared hosting. For "php artisan serve" use: composer run serve
upload_max_filesize = 10M
post_max_size = 350M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M

